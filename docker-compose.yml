version: '3.1'



services:


  reverse-proxy:
    image: jwilder/nginx-proxy:alpine
    container_name: 'Reverse-Proxy'
    restart: always
    ports:
      - '80:80'
    volumes:
      - certs:/etc/nginx/certs:ro
      - vhostd:/etc/nginx/vhost.d
      - html:/usr/share/nginx/html
      - /var/run/docker.sock:/tmp/docker.sock:ro


  angular:
    container_name: 'chatRealTimeAngular-App'
    build:
      context: ~/src/chatRealTimeAngular
      dockerfile: Dockerfile
    restart: always
    environment:
      - VIRTUAL_HOST=luiscg.net



  node:
    container_name: 'chatRealTimeNode-App'
    build:
      context: ~/src/chatRealTimeNode
      dockerfile: Dockerfile
    restart: always
    environment:
      - VIRTUAL_HOST=apinode.luiscg.net


volumes:
  certs:
  html:
  vhostd:
